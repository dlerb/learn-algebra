[
  {
    "kind": "equivalence",
    "id": "notation.explicit-vs-implicit-product",
    "group": "multiplication",
    "title": "Explicit vs. implicit multiplication",
    "priority": 1,
    "metaPatterns": ["meta.juxtaposition"],
    "conventions": ["conv.juxtaposition", "conv.canonical-order"],
    "note": "Symbols next to each other, or joined by $\\cdot$ or $\\times$, all mean multiply. $3x$ is a product — not a two-digit number, and not $3 + x$.",
    "equivalents": ["3 \\times x", "3 \\cdot x", "3x", "x \\cdot 3", "(3)(x)"],
    "pitfalls": [{ "expr": "3 + x", "cites": ["mis.juxtaposition-as-plus"] }]
  },
  {
    "kind": "equivalence",
    "id": "notation.product-with-brackets",
    "group": "multiplication",
    "title": "Multiplication with brackets",
    "priority": 14,
    "requires": ["notation.explicit-vs-implicit-product"],
    "metaPatterns": ["meta.juxtaposition", "meta.implicit-op-before-bracket"],
    "conventions": ["conv.juxtaposition", "conv.brackets-group"],
    "note": "A number against a bracket means multiply: 2(x+1) is 2 times (x+1), not 2 + (x+1).",
    "equivalents": ["2 \\times (x+1)", "2 \\cdot (x+1)", "2(x+1)", "(x+1) \\cdot 2"],
    "pitfalls": [{ "expr": "2 + (x+1)", "cites": ["mis.juxtaposition-as-plus"] }]
  },
  {
    "kind": "equivalence",
    "id": "notation.coefficient-negative-one",
    "group": "multiplication",
    "title": "Coefficient of −1",
    "priority": 3,
    "requires": ["notation.explicit-vs-implicit-product", "notation.negative-numbers"],
    "metaPatterns": ["meta.juxtaposition", "meta.three-minuses"],
    "justifiedBy": ["thm.negative-one-times"],
    "conventions": ["conv.invisible-one", "conv.juxtaposition"],
    "note": "An invisible coefficient of -1 means negate: (-1)a, -1a and -a are the same object. (Similarly a coefficient of 1 is invisible: 1x = x.)",
    "equivalents": ["(-1) \\cdot a", "(-1)a", "-1a", "-a"],
    "pitfalls": [{ "expr": "-1 + a", "cites": ["mis.juxtaposition-as-plus"] }]
  },
  {
    "kind": "equivalence",
    "id": "notation.coefficient-zero",
    "group": "multiplication",
    "title": "Coefficient of 0",
    "requires": ["notation.explicit-vs-implicit-product"],
    "metaPatterns": ["meta.juxtaposition"],
    "justifiedBy": ["thm.zero-product"],
    "conventions": ["conv.juxtaposition"],
    "note": "Zero times anything is zero: 0x = 0, regardless of x. It is not x.",
    "equivalents": ["0 \\cdot x", "0x", "0"],
    "pitfalls": [{ "expr": "x", "cites": ["mis.juxtaposition-as-plus"] }]
  },
  {
    "kind": "equivalence",
    "id": "notation.repeated-addition",
    "group": "multiplication",
    "title": "Repeated addition vs. multiplication",
    "priority": 10,
    "requires": ["notation.explicit-vs-implicit-product"],
    "metaPatterns": ["meta.juxtaposition"],
    "justifiedBy": ["def.integer-multiple"],
    "conventions": ["conv.juxtaposition"],
    "note": "Adding a term to itself scales it: $x + x = 2x$ (and $x+x+x = 3x$). Do not confuse with $x \\cdot x = x^2$.",
    "equivalents": ["x + x", "2x"],
    "pitfalls": [{ "expr": "x^2", "cites": ["anti.repetition-confusion"] }]
  }
]
